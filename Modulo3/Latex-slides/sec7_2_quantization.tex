% ============================================
% SEÇÃO 7.2: QUANTIZAÇÃO
% Derivações completas, passo a passo.
% ============================================

% ------------------------------------------------------------------
\subsection{Quantizador Uniforme}
% ------------------------------------------------------------------

\begin{frame}{Onde Estamos na Cadeia?}
  \drawpipeline{quant}
  \vspace{0.1cm}
  {\small Nesta seção: \textbf{Quantizador} — o sinal já está discreto no
  tempo; agora discretizamos a amplitude e analisamos o ruído introduzido.}
\end{frame}

\begin{frame}{Por Que Quantizar?}
  Após a amostragem, temos $g[n] = g(nT_s)$:
  discreto no tempo, mas com \textbf{amplitude ainda contínua}.

  \vspace{0.15cm}
  \begin{columns}[T]
    \column{0.50\textwidth}
    \textbf{O problema:}
    \begin{itemize}
      \item Um processador só armazena valores \alert{discretos}
      \item Com $n$ bits há apenas $2^n$ valores representáveis
      \item Amplitudes contínuas \textbf{precisam ser arredondadas}
    \end{itemize}

    \column{0.47\textwidth}
    \textbf{Analogia cotidiana:}
    \begin{itemize}
      \item Régua com marcas a cada 1 mm
      \item $23{,}731$ mm $\;\to\;$ $23{,}7$ mm
      \item O erro é inevitável — só reduzível
    \end{itemize}
  \end{columns}

  \vspace{0.15cm}
  \textbf{A questão central:}
  \begin{enumerate}
    \item Quanto ruído a quantização introduz?
    \item Como esse ruído depende do número de bits?
  \end{enumerate}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Quantização Uniforme: A Ideia}
  \begin{columns}[c]
    \column{0.46\textwidth}
    \begin{center}
    \begin{tikzpicture}[>=stealth, every node/.style={font=\scriptsize}]
      \def\h{0.45}
      \draw[->,gray!55] (0,-0.2) -- (0,3.95)
           node[above,black,font=\tiny]{amplitude};
      \foreach \k/\code in {0/000,1/001,2/010,3/011,4/100,5/101,6/110,7/111}{
        \pgfmathsetmacro\yb{\k*\h}
        \pgfmathsetmacro\ym{(\k+0.5)*\h}
        \draw[densely dotted,gray!50] (0,\yb) -- (3.2,\yb);
        \draw[UNB_GREEN, very thick] (0.15,\ym) -- (1.25,\ym);
        \fill[UNB_GREEN] (0.15,\ym) circle (1.1pt);
        \node[anchor=west, font=\tiny\ttfamily, gray!55!black]
             at (1.32,\ym) {\code};
      }
      \draw[densely dotted,gray!50] (0,8*\h) -- (3.2,8*\h);
      \node[left,font=\tiny] at (0,0) {$V_{\min}$};
      \node[left,font=\tiny] at (0,8*\h) {$V_{\max}$};
      % passo Delta
      \draw[<->, UNB_BLUE, thick] (2.75,3*\h) -- (2.75,4*\h);
      \node[UNB_BLUE, right, font=\scriptsize] at (2.75,3.5*\h) {$\Delta$};
      % amostra contínua -> nível mais próximo
      \fill[RED] (-0.95,2.18) circle (1.7pt);
      \node[left,font=\tiny,black] at (-0.95,2.18) {amostra};
      \draw[->,RED,thick] (-0.88,2.18) -- (0.12,2.025);
    \end{tikzpicture}
    \end{center}

    \column{0.54\textwidth}
    A quantização divide a faixa $[V_{\min}, V_{\max}]$ em
    \textbf{$L$ níveis igualmente espaçados}.

    \vspace{0.3cm}
    Cada amostra é \textbf{arredondada ao nível mais próximo} (o ponto
    médio do intervalo) e recebe um \textbf{código binário}.

    \vspace{0.3cm}
    Com $n$ bits há $L = 2^n$ níveis; o passo $\Delta$ é a distância
    entre níveis vizinhos.

    \vspace{0.3cm}
    \textit{A seguir, formalizamos $\Delta$ e os níveis com fórmulas.}
  \end{columns}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{O Quantizador Uniforme — Passo 1: A Faixa de Amplitude}
  O sinal amostrado $g[n]$ assume valores dentro de uma faixa finita
  $[V_{\min}, V_{\max}]$. A largura total é a tensão \textbf{pico a pico}:
  \[
    V_{pp} = V_{\max} - V_{\min}.
  \]

  \vspace{0.4cm}
  \begin{center}
  \begin{tikzpicture}[>=stealth, every node/.style={font=\scriptsize}]
    \draw[->,gray!55] (-0.5,0) -- (7.4,0) node[right,black]{$g$};
    \fill[UNB_GOLD!16] (0,0) rectangle (6.4,0.7);
    \draw[UNB_BLUE,very thick] (0,-0.12) -- (0,0.95);
    \draw[UNB_BLUE,very thick] (6.4,-0.12) -- (6.4,0.95);
    \node[below,black] at (0,-0.14) {$V_{\min}$};
    \node[below,black] at (6.4,-0.14) {$V_{\max}$};
    \draw[<->,thick,black] (0,1.25) -- (6.4,1.25)
        node[midway,above]{$V_{pp} = V_{\max}-V_{\min}$};
  \end{tikzpicture}
  \end{center}

  \vspace{0.3cm}
  \textit{Toda a informação de amplitude está contida nessa faixa; é ela
  que vamos dividir em níveis discretos.}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{O Quantizador Uniforme — Passo 2: Particionar em $L$ Intervalos}
  Dividimos a faixa $V_{pp}$ em \textbf{$L$ intervalos de mesma largura}.
  Como os $L$ degraus, somados, cobrem toda a faixa:
  \[
    L \cdot \Delta = V_{pp}
    \quad\Longrightarrow\quad
    \boxed{\;\Delta = \dfrac{V_{pp}}{L}\;}
  \]

  \vspace{0.35cm}
  \begin{center}
  \begin{tikzpicture}[>=stealth, every node/.style={font=\scriptsize}]
    \draw[->,gray!55] (-0.5,0) -- (7.4,0) node[right,black]{$g$};
    \fill[UNB_GOLD!12] (0,0) rectangle (6.4,0.7);
    \foreach \i in {0,...,8}
      \draw[gray!70, thick] (\i*0.8,0) -- (\i*0.8,0.7);
    \draw[UNB_BLUE,very thick] (0,-0.12)--(0,0.7);
    \draw[UNB_BLUE,very thick] (6.4,-0.12)--(6.4,0.7);
    \node[below,black] at (0,-0.14) {$V_{\min}$};
    \node[below,black] at (6.4,-0.14) {$V_{\max}$};
    \draw[<->,thick,UNB_BLUE] (0,1.0) -- (0.8,1.0)
        node[midway,above,black]{$\Delta$};
    \node[gray!60!black] at (3.2,-0.5) {$L$ intervalos iguais\, (aqui $L=8$)};
  \end{tikzpicture}
  \end{center}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{O Quantizador Uniforme — Passo 3: Os Níveis de Quantização}
  Cada intervalo é representado por um único valor: o seu \textbf{ponto
  médio}. O $k$-ésimo intervalo
  $[V_{\min}+k\Delta,\; V_{\min}+(k{+}1)\Delta)$ recebe o nível
  \[
    \hat{g} = V_{\min} + \left(k+\tfrac12\right)\Delta,
    \qquad k = 0,1,\dots,L-1.
  \]

  \vspace{0.25cm}
  \begin{center}
  \begin{tikzpicture}[>=stealth, every node/.style={font=\scriptsize}]
    \draw[->,gray!55] (-0.5,0) -- (7.4,0) node[right,black]{$g$};
    \foreach \i in {0,...,8}
      \draw[gray!55] (\i*0.8,0) -- (\i*0.8,0.7);
    \foreach \k in {0,...,7}{
      \pgfmathsetmacro\xm{(\k+0.5)*0.8}
      \draw[UNB_GREEN,very thick] (\xm,0.12)--(\xm,0.58);
      \fill[UNB_GREEN] (\xm,0.35) circle (2pt);
    }
    \node[below,black] at (0,-0.12) {$V_{\min}$};
    \node[below,black] at (6.4,-0.12) {$V_{\max}$};
    \node[UNB_GREEN!60!black] at (3.2,-0.5) {níveis $\hat{g}$ = pontos médios};
  \end{tikzpicture}
  \end{center}

  \vspace{0.05cm}
  {\footnotesize Com $n$ bits há $L = 2^n$ níveis, cada um com seu próprio
  código binário.}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{O Quantizador Uniforme — Detalhe de Um Intervalo}
  Ampliando o $k$-ésimo intervalo: ele vai do \textbf{limite inferior}
  ao \textbf{limite superior}, e o nível
  $\hat{g}=V_{\min}+\left(k+\tfrac12\right)\Delta$ fica exatamente no meio.

  \vspace{0.5cm}
  \begin{center}
  \begin{tikzpicture}[>=stealth, every node/.style={font=\scriptsize}]
    \def\xa{1.1}\def\xb{5.5}\def\xm{3.3}
    \fill[UNB_GOLD!18] (\xa,0) rectangle (\xb,0.85);
    \draw[->,gray!55] (-0.2,0) -- (6.8,0) node[right,black]{$g$};
    \draw[UNB_BLUE,very thick] (\xa,-0.15)--(\xa,0.85);
    \draw[UNB_BLUE,very thick] (\xb,-0.15)--(\xb,0.85);
    \draw[UNB_GREEN,very thick] (\xm,-0.15)--(\xm,0.85);
    \fill[UNB_GREEN] (\xm,0.42) circle (2.4pt);
    \node[UNB_GREEN!60!black, right] at (\xm+0.12,0.62) {$\hat{g}$};
    % passo Delta
    \draw[<->,thick,black] (\xa,1.15)--(\xb,1.15) node[midway,above]{$\Delta$};
    % rótulos dos limites
    \node[below,UNB_BLUE,align=center] at (\xa,-0.16)
        {$V_{\min}{+}k\Delta$\\(limite inf.)};
    \node[below,UNB_BLUE,align=center] at (\xb,-0.16)
        {$V_{\min}{+}(k{+}1)\Delta$\\(limite sup.)};
  \end{tikzpicture}
  \end{center}

  \vspace{0.45cm}
  {\footnotesize Qualquer amostra que caia neste intervalo é arredondada
  para o \textbf{mesmo} nível $\hat{g}$.}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{O Quantizador Uniforme — Exemplo Numérico}
  Sejam $V_{\min}=0$, $V_{\max}=8$ V e $L=8$ níveis ($n=3$ bits),
  logo $\Delta = 8/8 = 1$ V. Quantizemos a amostra $g[n] = 5{,}3$ V.

  \vspace{0.2cm}
  \begin{center}
  \begin{tikzpicture}[>=stealth, every node/.style={font=\scriptsize}]
    \draw[->,gray!55] (-0.3,0) -- (8.8,0) node[right,black]{$g$ (V)};
    % intervalo destacado [5,6)
    \fill[UNB_GOLD!28] (5,0) rectangle (6,0.7);
    \foreach \i in {0,...,8}{
      \draw[gray!55] (\i,0)--(\i,0.7);
      \node[below,gray!70!black,font=\tiny] at (\i,-0.02) {\i};
    }
    % níveis (midpoints)
    \foreach \k in {0,...,7}
      \fill[UNB_GREEN!45] (\k+0.5,0.35) circle (1.4pt);
    % nível do intervalo destacado
    \draw[UNB_GREEN,very thick] (5.5,0.08)--(5.5,0.62);
    \fill[UNB_GREEN] (5.5,0.35) circle (2.3pt);
    % amostra e seta
    \fill[RED] (5.3,1.0) circle (2pt);
    \node[above,RED!80!black] at (5.3,1.08) {$g=5{,}3$ V};
    \draw[->,RED,thick] (5.3,0.92) -- (5.5,0.42);
    \node[below,UNB_GREEN!58!black] at (5.5,-0.42) {$\hat{g}=5{,}5$ V};
  \end{tikzpicture}
  \end{center}

  \vspace{0.1cm}
  {\footnotesize A amostra $5{,}3$ cai no intervalo $[5,6)$
  $\Rightarrow$ nível $\hat{g}=5{,}5$ V, com erro
  $e_q = \hat{g}-g = +0{,}2$ V.}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Característica do Quantizador}
  \begin{center}
    \includegraphics[width=0.70\textwidth]{figures/cap7/quantization_illustration}
  \end{center}
  \vspace{-0.05cm}
  {\footnotesize
    \textit{(a)} Função entrada-saída (escada); o passo $\Delta$ é o
    tamanho de cada degrau.\quad
    \textit{(b)} Sinal original (azul) vs.\ quantizado (verde); barras
    roxas indicam o erro em cada amostra.
  }
\end{frame}

% ------------------------------------------------------------------
\subsection{Ruído de Quantização}
% ------------------------------------------------------------------

\begin{frame}{O Erro de Quantização}
  O \textbf{erro de quantização} é a diferença entre o valor
  quantizado e o valor real:
  \[
    e_q[n] = \hat{g}[n] - g[n]
  \]

  \vspace{0.3cm}
  Como o quantizador mapeia cada amostra para o
  \textbf{ponto médio} de seu intervalo $\Delta$,
  o erro fica sempre limitado a:
  \[
    -\frac{\Delta}{2} \;\le\; e_q[n] \;\le\; +\frac{\Delta}{2}
  \]

  \vspace{0.4cm}
  \textbf{Compromisso fundamental:}
  \begin{itemize}
    \item Erro menor $\Rightarrow$ $\Delta$ menor
          $\Rightarrow$ mais níveis $L = V_{pp}/\Delta$
          $\Rightarrow$ mais bits $n = \log_2 L$
    \item Aumentar $n$ em 1 bit $\Rightarrow$ $\Delta$ cai pela metade
          $\Rightarrow$ erro máximo cai pela metade
  \end{itemize}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Modelo Estatístico do Erro de Quantização}
  \textbf{Hipóteses do modelo de ruído de quantização}
  (válidas para $L$ grande e sinal variando rapidamente):

  \begin{enumerate}
    \item $e_q[n]$ é uma \textbf{variável aleatória uniforme} em
          $[-\Delta/2,\; +\Delta/2]$
    \item Erros em amostras consecutivas são \textbf{não-correlacionados}
    \item O erro é \textbf{independente} do sinal de entrada
  \end{enumerate}

  \vspace{0.3cm}
  Sob essas hipóteses, a função de densidade de probabilidade (PDF) é:
  \[
    p(e_q) =
    \begin{cases}
      \dfrac{1}{\Delta}, & \text{se } -\dfrac{\Delta}{2}
                            \le e_q \le \dfrac{\Delta}{2} \\[8pt]
      0,                 & \text{caso contrário}
    \end{cases}
  \]

  Cada valor do erro é \textbf{igualmente provável} dentro do intervalo.
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{PDF do Erro de Quantização}
  \begin{center}
    \includegraphics[width=0.60\textwidth]{figures/cap7/quantization_error_pdf}
  \end{center}
  \begin{center}
  {\footnotesize Erro $e_q$ uniformemente distribuído em
  $[-\Delta/2,\,+\Delta/2]$, com densidade constante $p(e_q) = 1/\Delta$.}
  \end{center}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{PDF do Erro de Quantização — Verificações}
  O modelo uniforme $p(e_q) = 1/\Delta$ em $[-\Delta/2,\,+\Delta/2]$
  satisfaz as duas propriedades esperadas de uma densidade:

  \vspace{0.6cm}
  \textbf{1. Área unitária} (é uma densidade de probabilidade válida):
  \[
    \int_{-\Delta/2}^{\Delta/2}\frac{1}{\Delta}\,de_q = 1
    \qquad(\checkmark)
  \]

  \vspace{0.4cm}
  \textbf{2. Média nula} (erro sem viés — tão provável para cima quanto
  para baixo):
  \[
    E[e_q] = \int_{-\Delta/2}^{\Delta/2} e_q \cdot\frac{1}{\Delta}\,de_q = 0
    \qquad(\checkmark)
  \]
\end{frame}

% ------------------------------------------------------------------
\subsection{Derivação de $P_q = \Delta^2/12$}
% ------------------------------------------------------------------

\begin{frame}{Derivação de $P_q$ — Passo 1: Definição Formal}
  A \textbf{potência do ruído de quantização} $P_q$ é o valor esperado
  do \emph{quadrado} do erro:
  \[
    P_q = E\!\left[e_q^2\right]
  \]

  \vspace{0.3cm}
  Para uma variável aleatória contínua, o valor esperado de uma função
  $f(e_q)$ é calculado como:
  \[
    E\!\left[f(e_q)\right] = \int_{-\infty}^{+\infty} f(e_q)\,p(e_q)\,de_q
  \]

  \vspace{0.3cm}
  Portanto, a definição formal de $P_q$ é:
  \[
    P_q = \int_{-\infty}^{+\infty} e_q^2 \; p(e_q) \; de_q
  \]

  \textit{Interpretação:} pesamos $e_q^2$ pela sua
  probabilidade $p(e_q)$ e somamos (integramos) sobre todos os valores.
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Derivação de $P_q$ — Passo 2: Substituição da PDF}
  Como $p(e_q) = 1/\Delta$ somente no intervalo
  $[-\Delta/2, +\Delta/2]$ e zero fora dele,
  podemos restringir os limites da integral:
  \[
    P_q = \int_{-\Delta/2}^{+\Delta/2} e_q^2 \cdot \frac{1}{\Delta} \; de_q
  \]

  \vspace{0.6cm}
  A constante $1/\Delta$ não depende de $e_q$ e pode sair da integral:
  \[
    P_q = \frac{1}{\Delta}
          \int_{-\Delta/2}^{+\Delta/2} e_q^2 \; de_q
  \]
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Derivação de $P_q$ — Passo 3: Resolver a Integral}
  Partindo de
  $P_q = \dfrac{1}{\Delta}\displaystyle\int_{-\Delta/2}^{+\Delta/2} e_q^2 \, de_q$,
  a primitiva de $e_q^2$ é $e_q^3/3$.

  \vspace{0.5cm}
  Aplicando o teorema fundamental do cálculo:
  \[
    P_q = \frac{1}{\Delta}
          \left[\frac{e_q^3}{3}\right]_{-\Delta/2}^{+\Delta/2}
        = \frac{1}{\Delta}
          \cdot\frac{\left(\Delta/2\right)^3 - \left(-\Delta/2\right)^3}{3}
  \]
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Derivação de $P_q$ — Passo 4: Simplificação}
  Calculamos os cubos:
  \[
    \left(\frac{\Delta}{2}\right)^{\!3} = \frac{\Delta^3}{8},
    \qquad
    \left(-\frac{\Delta}{2}\right)^{\!3} = -\frac{\Delta^3}{8}
  \]

  \vspace{0.3cm}
  Substituindo na expressão anterior:
  \[
    P_q = \frac{1}{\Delta}
          \cdot\frac{\;\dfrac{\Delta^3}{8} -
                     \left(-\dfrac{\Delta^3}{8}\right)}{3}
        = \frac{1}{\Delta}
          \cdot\frac{\;2\cdot\dfrac{\Delta^3}{8}\;}{3}
        = \frac{1}{\Delta}\cdot\frac{\Delta^3}{12}
  \]

  \vspace{0.3cm}
  Cancelando um fator $\Delta$, chegamos ao resultado:
  \[
    \boxed{\,P_q = \frac{\Delta^2}{12}\,}
  \]
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Potência do Ruído de Quantização — Interpretação}
  \begin{block}{Potência do Ruído de Quantização}
    \[
      P_q = \frac{\Delta^2}{12}
    \]
    O ruído depende \textbf{apenas} do passo $\Delta$ — quanto mais fino
    o degrau, menor a potência do erro.
  \end{block}

  \vspace{0.4cm}
  \textbf{Consequência prática:}
  \begin{itemize}
    \item Para reduzir $P_q$ à metade ($-3$ dB), reduz-se $\Delta$ por
          $\sqrt{2}$, ou seja, dobra-se o número de níveis $L$.
    \item Dobrar $L$ corresponde a adicionar \textbf{1 bit} de resolução.
    \item Exemplo: um ADC de 12 bits tem $\Delta$ 16$\times$ menor que um
          de 8 bits $\Rightarrow$ $P_q$ é $256\times$ menor ($-24$ dB).
  \end{itemize}
\end{frame}

% ------------------------------------------------------------------
\subsection{SQNR e Regra dos 6 dB}
% ------------------------------------------------------------------

\begin{frame}{Relação Sinal-Ruído de Quantização (SQNR)}
  A qualidade da quantização é medida pelo \textbf{SQNR}
  (\textit{Signal-to-Quantization-Noise Ratio}):
  \[
    \SQNR = \frac{P_s}{P_q}
           = \frac{P_s}{\Delta^2/12}
           = \frac{12\,P_s}{\Delta^2}
  \]

  \vspace{0.3cm}
  Expressando $\Delta$ em termos do número de bits $n$
  ($L = 2^n$ níveis, faixa $V_{pp}$):
  \[
    \Delta = \frac{V_{pp}}{2^n}
    \;\Longrightarrow\;
    \SQNR = \frac{12\,P_s}{\left(V_{pp}/2^n\right)^2}
           = \frac{12\,P_s}{V_{pp}^2}\cdot 2^{2n}
  \]

  \vspace{0.3cm}
  \textbf{Observação:} $2^{2n} = 4^n$.
  Adicionar 1 bit multiplica o SQNR por 4 (ou seja, $+6$ dB).
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{SQNR para um Sinal Senoidal — Derivação}
  \textbf{Caso prático:} senoide $g(t) = A\cos(\omega t)$ que ocupa
  \textbf{toda a faixa} do quantizador.

  \vspace{0.3cm}
  Parâmetros:
  \[
    V_{pp} = A - (-A) = 2A,
    \qquad
    P_s = \frac{A^2}{2}
    \quad\text{(potência média da senoide)}
  \]

  \vspace{0.3cm}
  Substituindo na expressão geral do SQNR:
  \[
    \SQNR = \frac{12\cdot(A^2/2)}{(2A)^2}\cdot 2^{2n}
           = \frac{6A^2}{4A^2}\cdot 2^{2n}
           = \frac{3}{2}\cdot 2^{2n}
           = 1{,}5\cdot 4^n
  \]

  \vspace{0.3cm}
  Convertendo para decibéis ($X_{\mathrm{dB}} = 10\log_{10}X$):
  \[
    \SQNR_{\mathrm{dB}}
    = 10\log_{10}\!\left(1{,}5\cdot 4^n\right)
    = 10\log_{10}(1{,}5) + n\cdot 10\log_{10}(4)
  \]
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{A Regra dos 6 dB por Bit}
  Calculando os valores numéricos dos logaritmos:
  \[
    10\log_{10}(1{,}5) \approx 1{,}76\;\text{dB},
    \qquad
    10\log_{10}(4)     \approx 6{,}02\;\text{dB}
  \]

  \vspace{0.2cm}
  Portanto:
  \[
    \SQNR_{\mathrm{dB}} \approx 1{,}76 + 6{,}02\,n
  \]

  \begin{block}{Regra dos 6 dB/bit (senoide em faixa cheia)}
    \[
      \SQNR_{\mathrm{dB}} \approx 1{,}76 + 6{,}02\,n\;\text{dB}
    \]
    Cada bit adicional eleva o SQNR em \textbf{$\approx 6$ dB}.
  \end{block}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Regra dos 6 dB/bit — Exemplos de Engenharia}
  Aplicando $\SQNR_{\mathrm{dB}} \approx 1{,}76 + 6{,}02\,n$ a sistemas reais:

  \vspace{0.3cm}
  \begin{itemize}
    \item \textbf{Telefonia PCM} ($n = 8$ bits): $\SQNR \approx 50\,\text{dB}$
          — suficiente para voz inteligível na rede telefônica.
    \item \textbf{CD de áudio} ($n = 16$ bits): $\SQNR \approx 98\,\text{dB}$
          — cobre toda a faixa dinâmica da audição humana.
    \item \textbf{Estúdio profissional} ($n = 24$ bits):
          $\SQNR \approx 146\,\text{dB}$ — margem para edição e mixagem
          sem acúmulo audível de ruído.
  \end{itemize}

  \vspace{0.3cm}
  \begin{block}{Critério de projeto}
    Para atingir um SQNR-alvo, o número de bits necessário é
    $n \ge \dfrac{\SQNR_{\mathrm{dB}} - 1{,}76}{6{,}02}$.
    Ex.: para 70 dB $\Rightarrow$ $n \ge 11{,}3 \Rightarrow$ 12 bits.
  \end{block}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{SQNR vs.\ Número de Bits — Confirmação Numérica}
  \begin{center}
    \includegraphics[width=0.68\textwidth]{figures/cap7/sqnr_vs_bits}
  \end{center}
  \vspace{0.0cm}
  {\footnotesize
    Simulação com senoide de $10^6$ amostras confirma a fórmula
    $1{,}76 + 6{,}02\,n$ dB com excelente precisão para $n \ge 4$ bits.
  }
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Efeito da Resolução na Qualidade do Sinal}
  \begin{center}
    \includegraphics[width=0.84\textwidth]{figures/cap7/quantization_resolution}
  \end{center}
  \vspace{-0.15cm}
  {\footnotesize
    Com $n = 2$ bits a forma de onda é severamente distorcida.
    Com $n = 8$ bits a diferença para o sinal original é imperceptível.
  }
\end{frame}

% ------------------------------------------------------------------

\begin{frame}[fragile]{Implementação em Python}
\begin{lstlisting}[language=Python]
import numpy as np

def quantize_uniform(x, n_bits, v_min=-1.0, v_max=1.0):
    L     = 2**n_bits                        # numero de niveis
    delta = (v_max - v_min) / L             # tamanho do passo
    x_c   = np.clip(x, v_min, v_max)        # limitar faixa
    idx   = np.floor((x_c - v_min)/delta).astype(int)
    idx   = np.clip(idx, 0, L-1)
    return v_min + (idx + 0.5) * delta       # ponto medio

# Calcular SQNR para varios numeros de bits
t = np.linspace(0, 100, 1_000_000)
g = np.sin(2*np.pi*t)                       # senoide em faixa cheia

for n in [4, 8, 16]:
    g_q  = quantize_uniform(g, n)
    sqnr = 10 * np.log10(np.mean(g**2) / np.mean((g_q-g)**2))
    teo  = 1.76 + 6.02*n
    print(f"n={n:2d}: SQNR = {sqnr:.1f} dB  (teoria: {teo:.1f} dB)")
\end{lstlisting}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}[fragile]{Implementação em Python — Resultado}
  Saída do script para $n \in \{4, 8, 16\}$ bits:

  \vspace{0.4cm}
  \begin{center}
  \begin{minipage}{0.7\textwidth}
  {\ttfamily\small
  n=\phantom{0}4: SQNR = 25.9 dB \;\; (teoria: 25.8 dB)\\
  n=\phantom{0}8: SQNR = 49.9 dB \;\; (teoria: 49.9 dB)\\
  n=16: SQNR = 98.1 dB \;\; (teoria: 98.1 dB)}
  \end{minipage}
  \end{center}

  \vspace{0.4cm}
  \begin{itemize}
    \item A simulação reproduz a teoria com erro $< 0{,}1$ dB.
    \item Cada salto de 8 bits adiciona $\approx 48$ dB
          ($8 \times 6{,}02$ dB) — consistente com a regra dos 6 dB/bit.
  \end{itemize}
\end{frame}

% ------------------------------------------------------------------
\subsection{Quantização Não-Uniforme e Lloyd-Max}
% ------------------------------------------------------------------

\begin{frame}{A Voz Não Ocupa a Faixa Uniformemente}
  Num sinal de voz a amplitude fica \textbf{quase sempre pequena}, com
  \textbf{poucos picos} grandes:

  \vspace{0.1cm}
  \begin{center}
  \begin{tikzpicture}[>=stealth, every node/.style={font=\scriptsize},
     declare function={
        env(\t)= 0.10 + 0.78*exp(-(\t-2)^2/0.35) + 0.55*exp(-(\t-4.6)^2/0.25);
        vz(\t)= env(\t)*sin(720*\t);}]
    % ----- forma de onda -----
    \draw[->,gray!55] (-0.2,0) -- (6.6,0) node[right,black]{$t$};
    \draw[->,gray!55] (0,-1.15) -- (0,1.2) node[left,black]{ampl.};
    \draw[UNB_BLUE, thick] plot[domain=0:6.3, samples=260] (\x, {vz(\x)});
    \node[gray!60!black] at (0.95,0.4) {silêncio};
    \node[UNB_BLUE!70!black] at (2.0,1.05) {pico};
    % ----- PDF lateral (concentrada em zero) -----
    \begin{scope}[xshift=7.6cm]
      \draw[->,gray!55] (0,-1.15) -- (0,1.2) node[left,black]{ampl.};
      \draw[->,gray!55] (-0.05,0) -- (2.7,0)
            node[right,black]{$p(\text{ampl.})$};
      \fill[UNB_GOLD!22] plot[domain=-1:1,samples=90] ({2.2*exp(-abs(\x)/0.22)},\x)
            -- (0,1) -- (0,-1) -- cycle;
      \draw[UNB_GOLD!85!black, very thick]
            plot[domain=-1:1,samples=90] ({2.2*exp(-abs(\x)/0.22)},\x);
      \node[UNB_GOLD!80!black, align=center] at (1.55,0.6)
            {densidade de\\probabilidade};
    \end{scope}
  \end{tikzpicture}
  \end{center}

  \vspace{0.05cm}
  {\footnotesize $p(\text{ampl.})$ é a \textbf{densidade de probabilidade}
  da amplitude: quanto maior, mais frequente é aquele valor. A distribuição
  é \textbf{concentrada perto de zero} — longe de ser uniforme.}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Uniforme É Ruim para Sinais Fracos}
  Os níveis uniformes se espalham por \textbf{toda} a faixa, mas o sinal
  vive \textbf{perto de zero} — só uns poucos níveis são realmente usados:

  \vspace{0.1cm}
  \begin{center}
  \begin{tikzpicture}[>=stealth, every node/.style={font=\scriptsize}]
    \draw[->,gray!55] (0,-1.25) -- (0,1.35) node[above,black]{amplitude};
    % níveis uniformes
    \foreach \y in {-0.875,-0.625,-0.375,-0.125,0.125,0.375,0.625,0.875}{
       \draw[UNB_GREEN!70!black, densely dashed] (0,\y) -- (3.0,\y);
       \fill[UNB_GREEN] (0,\y) circle (1.3pt);
    }
    % PDF lateral
    \fill[UNB_GOLD!18] plot[domain=-1:1,samples=90] ({2.6*exp(-abs(\x)/0.24)},\x)
          -- (0,1) -- (0,-1) -- cycle;
    \draw[UNB_GOLD!85!black, very thick]
          plot[domain=-1:1,samples=90] ({2.6*exp(-abs(\x)/0.24)},\x);
    \node[UNB_GOLD!80!black] at (2.0,0.6) {$p(\text{ampl.})$};
    % regiao de alta densidade com poucos niveis
    \draw[<->,RED,thick] (3.25,-0.18) -- (3.25,0.18);
    \node[RED!80!black, right, align=left] at (3.35,0) {muitas amostras,\\poucos níveis};
  \end{tikzpicture}
  \end{center}

  \vspace{0.05cm}
  {\footnotesize Para sinais fracos sobram poucos níveis $\Rightarrow$ erro
  relativo alto $\Rightarrow$ \alert{SQNR baixo}. Os níveis das amplitudes
  grandes quase não são usados.}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Solução: Quantização Não-Uniforme}
  Colocamos \textbf{mais níveis onde o sinal é mais provável} (perto de
  zero) e menos onde é raro (amplitudes grandes):

  \vspace{0.1cm}
  \begin{center}
  \begin{tikzpicture}[>=stealth, every node/.style={font=\scriptsize}]
    \draw[->,gray!55] (0,-1.25) -- (0,1.35) node[above,black]{amplitude};
    % níveis não-uniformes (densos perto de zero)
    \foreach \y in {-0.92,-0.5,-0.27,-0.09,0.09,0.27,0.5,0.92}{
       \draw[UNB_GREEN!70!black, densely dashed] (0,\y) -- (3.0,\y);
       \fill[UNB_GREEN] (0,\y) circle (1.3pt);
    }
    % PDF lateral
    \fill[UNB_GOLD!18] plot[domain=-1:1,samples=90] ({2.6*exp(-abs(\x)/0.24)},\x)
          -- (0,1) -- (0,-1) -- cycle;
    \draw[UNB_GOLD!85!black, very thick]
          plot[domain=-1:1,samples=90] ({2.6*exp(-abs(\x)/0.24)},\x);
    \node[UNB_GOLD!80!black] at (2.0,0.6) {$p(\text{ampl.})$};
    \draw[<->,UNB_GREEN!60!black,thick] (3.25,-0.18) -- (3.25,0.18);
    \node[UNB_GREEN!55!black, right, align=left] at (3.35,0) {níveis finos\\onde importa};
  \end{tikzpicture}
  \end{center}

  \vspace{0.05cm}
  {\footnotesize Assim o \textbf{erro relativo} fica aproximadamente
  constante $\Rightarrow$ \textbf{SQNR quase constante} em toda a faixa
  dinâmica.}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Como Calcular os Níveis Ótimos: Lloyd-Max}
  O quantizador ótimo minimiza o erro quadrático médio
  $E\!\left[(x-\hat{x})^2\right]$ dada a densidade $p(x)$, satisfazendo
  \textbf{duas condições}:

  \vspace{0.15cm}
  \textbf{1. Cada nível é o centróide (média) do seu intervalo:}
  \[
    \hat{y}_k = \frac{\displaystyle\int_{b_{k-1}}^{b_k} x\,p(x)\,dx}
                     {\displaystyle\int_{b_{k-1}}^{b_k} p(x)\,dx}
  \]

  \textbf{2. Cada fronteira é o ponto médio entre níveis vizinhos:}
  \[
    b_k = \frac{\hat{y}_k + \hat{y}_{k+1}}{2}
  \]

  \vspace{-0.05cm}
  {\footnotesize \textbf{Algoritmo de Lloyd:} alterna entre as duas
  condições até convergir.}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Demonstração — Condição 1: Nível é o Centróide}
  A distorção (erro quadrático médio) é a soma sobre os intervalos:
  \[
    D = \sum_{k} \int_{b_{k-1}}^{b_k} \left(x - \hat{y}_k\right)^2 p(x)\,dx.
  \]

  Com as fronteiras fixas, fazemos $\partial D / \partial \hat{y}_k = 0$:
  \[
    \int_{b_{k-1}}^{b_k} -2\left(x - \hat{y}_k\right) p(x)\,dx = 0
    \;\Longrightarrow\;
    \int_{b_{k-1}}^{b_k}\! x\,p(x)\,dx = \hat{y}_k \int_{b_{k-1}}^{b_k}\! p(x)\,dx.
  \]

  Isolando $\hat{y}_k$, obtemos o \textbf{centróide}:
  \[
    \boxed{\;\hat{y}_k = \left.\int_{b_{k-1}}^{b_k} x\,p(x)\,dx \;\middle/\;
             \int_{b_{k-1}}^{b_k} p(x)\,dx\;\right.}
  \]
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Demonstração — Condição 2: A Derivada de Leibniz}
  A fronteira $b_k$ é \textbf{limite superior} do 1º termo e
  \textbf{limite inferior} do 2º termo da distorção:
  \[
    D_k = \int_{b_{k-1}}^{b_k}\!\!\left(x-\hat{y}_k\right)^2 p(x)\,dx
        + \int_{b_k}^{b_{k+1}}\!\!\left(x-\hat{y}_{k+1}\right)^2 p(x)\,dx.
  \]

  \textbf{Regra de Leibniz:} derivar em relação a um limite devolve o
  integrando avaliado nele — com sinal $+$ no superior, $-$ no inferior:
  \[
    \frac{\partial}{\partial b_k}\!\int_{a}^{b_k}\! f(x)\,dx = f(b_k),
    \qquad
    \frac{\partial}{\partial b_k}\!\int_{b_k}^{c}\! f(x)\,dx = -\,f(b_k).
  \]

  Aqui o integrando é $f(x) = (x-\hat{y})^2 p(x)$, avaliado em $x=b_k$:
  \[
    \frac{\partial D}{\partial b_k}
    = \underbrace{\left(b_k-\hat{y}_k\right)^2 p(b_k)}_{\text{1º termo }(+)}
    \;-\;
    \underbrace{\left(b_k-\hat{y}_{k+1}\right)^2 p(b_k)}_{\text{2º termo }(-)}.
  \]
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Demonstração — Condição 2: O Ponto Médio}
  No ótimo, $\dfrac{\partial D}{\partial b_k} = 0$, logo:
  \[
    \left(b_k-\hat{y}_k\right)^2 p(b_k)
    = \left(b_k-\hat{y}_{k+1}\right)^2 p(b_k).
  \]

  Como $p(b_k) > 0$, cancelamos a densidade:
  \[
    \left(b_k-\hat{y}_k\right)^2 = \left(b_k-\hat{y}_{k+1}\right)^2.
  \]

  Como $\hat{y}_k < b_k < \hat{y}_{k+1}$, temos $b_k-\hat{y}_k > 0$ e
  $b_k-\hat{y}_{k+1} < 0$; a raiz válida é
  $b_k-\hat{y}_k = \hat{y}_{k+1}-b_k$. Isolando $b_k$ — o
  \textbf{ponto médio}:
  \[
    \boxed{\;b_k = \frac{\hat{y}_k + \hat{y}_{k+1}}{2}\;}
  \]
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Exemplo: Nível Ótimo de Um Intervalo}
  Seja o intervalo $[0, a]$ com densidade \textbf{decrescente}
  $p(x) = \dfrac{2(a-x)}{a^2}$ (amplitudes pequenas mais prováveis).

  \vspace{0.2cm}
  O nível ótimo é o \textbf{centróide} do intervalo:
  \[
    \hat{y} = \frac{\displaystyle\int_0^a x\,p(x)\,dx}
                   {\displaystyle\int_0^a p(x)\,dx}
            = \frac{\dfrac{2}{a^2}\displaystyle\int_0^a x\,(a-x)\,dx}{1}
            = \frac{2}{a^2}\cdot\frac{a^3}{6}
            = \boxed{\dfrac{a}{3}}
  \]

  \vspace{0.15cm}
  \textbf{Interpretação:} o nível ótimo cai em $a/3$, \textbf{deslocado
  para perto de zero} — e não no meio geométrico $a/2$ — acompanhando a
  concentração da densidade.
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Exemplo Prático: O Sinal de um Sensor e sua PDF}
  \textbf{Cenário:} um sensor (vibração / áudio) gera um sinal em $[0,4]$ V.
  Ele fica quase sempre em amplitudes pequenas (repouso / sinal fraco) e
  raramente em amplitudes grandes (eventos fortes). Modelo:
  $p(x) = \dfrac{4-x}{8}$.

  \vspace{0.05cm}
  \begin{columns}[c]
    \column{0.52\textwidth}
    \begin{center}
    \begin{tikzpicture}[>=stealth, x=1.15cm, y=2.1cm,
        every node/.style={font=\scriptsize}]
      \draw[->,gray!55] (-0.2,0) -- (4.6,0) node[right,black]{$x$ (V)};
      \draw[->,gray!55] (0,-0.04) -- (0,0.62) node[above,black]{$p(x)$};
      \fill[UNB_GOLD!12] (0,0) -- (0,0.5) -- (4,0) -- cycle;
      \fill[UNB_GREEN!28] (0,0) -- (0,0.5) -- (1,0.375) -- (1,0) -- cycle;
      \draw[UNB_GOLD!85!black, very thick] (0,0.5) -- (4,0);
      \node[UNB_GOLD!80!black] at (2.7,0.30) {$\frac{4-x}{8}$};
      \node[UNB_GREEN!50!black, align=center, font=\tiny] at (0.5,0.15)
           {44\%\\do tempo};
      \node[gray, font=\tiny] at (3.5,0.10) {6\%};
      \foreach \xv in {0,1,2,3,4}
        \draw (\xv,0.012)--(\xv,-0.012) node[below,font=\tiny]{\xv};
    \end{tikzpicture}
    \end{center}

    \column{0.48\textwidth}
    {\footnotesize
    $p(x)$ é a \textbf{densidade de probabilidade} da amplitude: a
    \textbf{área} sob $p(x)$ numa faixa de tensão é a \textbf{fração do
    tempo} que o sinal passa nela.

    \vspace{0.2cm}
    Aqui, $44\%$ do tempo em $[0,1]$ V, mas só $6\%$ em $[3,4]$ V — por
    isso a quantização uniforme desperdiça níveis nas amplitudes altas.}
  \end{columns}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Exemplo Prático: Níveis Ótimos (Centróides)}
  Quantizador de \textbf{2 níveis} (1 bit), partindo da fronteira uniforme
  $b = 2$ V. Cada nível é o \textbf{centróide} do seu lado:
  \[
    \hat{y}_1 = \frac{\displaystyle\int_0^2 x\,p\,dx}{\displaystyle\int_0^2 p\,dx}
              = \frac{2/3}{3/4} = \frac{8}{9} \approx 0{,}89\ \text{V},
    \quad
    \hat{y}_2 = \frac{\displaystyle\int_2^4 x\,p\,dx}{\displaystyle\int_2^4 p\,dx}
              = \frac{2/3}{1/4} = \frac{8}{3} \approx 2{,}67\ \text{V}.
  \]

  \vspace{0.05cm}
  \begin{center}
  \begin{tikzpicture}[>=stealth, x=1.25cm, y=1.7cm,
      every node/.style={font=\scriptsize}]
    \draw[->,gray!55] (-0.2,0) -- (4.7,0) node[right,black]{$x$ (V)};
    \fill[UNB_GOLD!12] (0,0) -- (0,0.5) -- (4,0) -- cycle;
    \draw[UNB_GOLD!85!black, very thick] (0,0.5) -- (4,0);
    \draw[UNB_BLUE, densely dashed, thick] (2,0) -- (2,0.5)
         node[above,black,font=\tiny]{$b=2$};
    % níveis ótimos (verde)
    \fill[UNB_GREEN] (0.89,0) circle (1.7pt);
    \fill[UNB_GREEN] (2.67,0) circle (1.7pt);
    \node[UNB_GREEN!50!black,font=\tiny,below=1pt] at (0.89,0) {$\hat y_1$};
    \node[UNB_GREEN!50!black,font=\tiny,below=1pt] at (2.67,0) {$\hat y_2$};
    % meios geométricos (cinza)
    \draw[gray!70] (1,0.05)--(1,-0.05); \node[gray,font=\tiny,above] at (1,0.05){$1{,}0$};
    \draw[gray!70] (3,0.05)--(3,-0.05); \node[gray,font=\tiny,above] at (3,0.05){$3{,}0$};
  \end{tikzpicture}
  \end{center}

  \vspace{-0.05cm}
  {\footnotesize Os níveis (verde) ficam \textbf{abaixo} dos meios
  geométricos (cinza, $1{,}0$ e $3{,}0$ V): migram para onde há mais sinal.}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Exemplo Prático: Atualizar a Fronteira (Lloyd)}
  \textbf{Atualizar a fronteira} (ponto médio dos novos níveis):
  \[
    b = \frac{\hat{y}_1 + \hat{y}_2}{2}
      = \frac{1}{2}\!\left(\frac{8}{9} + \frac{8}{3}\right)
      = \frac{16}{9} \approx 1{,}78\ \text{V}.
  \]

  A fronteira \textbf{migrou de $2{,}0$ para $1{,}78$ V} — para perto de
  zero, onde o sinal passa mais tempo. \textbf{Iterando} (recalcular
  centróides $\to$ atualizar fronteira), o algoritmo de Lloyd converge.

  \vspace{0.2cm}
  \begin{block}{}
  O resultado é um quantizador \textbf{não-uniforme} com degraus finos
  perto de zero e largos nas amplitudes altas — exatamente o efeito que o
  \textbf{companding} ($\mu$-law / A-law) produz na prática.
  \end{block}
\end{frame}

% ------------------------------------------------------------------

\begin{frame}{Resultado Final: Ótimo vs.\ Uniforme}
  Após a convergência do algoritmo de Lloyd, comparamos o quantizador
  \textbf{ótimo} com o \textbf{uniforme} (mesmo número de níveis):

  \vspace{0.1cm}
  \begin{columns}[c]
    \column{0.46\textwidth}
    \footnotesize
    \renewcommand{\arraystretch}{1.25}
    \begin{tabular}{lcc}
      \hline
                       & \textbf{Uniforme} & \textbf{Ótimo} \\
      \hline
      Limiar $b$       & $2{,}00$ V & $1{,}53$ V \\
      Nível $\hat y_1$ & $1{,}00$ V & $0{,}70$ V \\
      Nível $\hat y_2$ & $3{,}00$ V & $2{,}35$ V \\
      Ruído $D$        & $0{,}333$  & $0{,}247$ \\
      \hline
    \end{tabular}

    \vspace{0.25cm}
    \textbf{Ganho de SQNR:}
    \[
      10\log_{10}\!\frac{D_{\text{unif}}}{D_{\text{ot}}}
      = 10\log_{10} 1{,}35 \approx \boxed{+1{,}3\ \text{dB}}
    \]
    {\footnotesize (potência do ruído $\approx 26\%$ menor)}

    \column{0.54\textwidth}
    \begin{center}
    \begin{tikzpicture}[>=stealth, x=1.15cm, y=1.7cm,
        every node/.style={font=\scriptsize}]
      \draw[->,gray!55] (-0.2,0)--(4.7,0) node[right,black]{$x$ (V)};
      \fill[UNB_GOLD!12] (0,0)--(0,0.5)--(4,0)--cycle;
      \draw[UNB_GOLD!85!black, very thick] (0,0.5)--(4,0);
      % uniforme (cinza)
      \draw[gray!75, densely dashed] (2,0)--(2,0.46);
      \fill[gray!75] (1,0) circle (1.6pt);
      \fill[gray!75] (3,0) circle (1.6pt);
      \node[gray!75,font=\tiny,above right=-1pt] at (2,0.42) {$b_u$};
      % ótimo (azul/verde)
      \draw[UNB_BLUE, densely dashed, thick] (1.53,0)--(1.53,0.46);
      \fill[UNB_GREEN] (0.70,0) circle (1.9pt);
      \fill[UNB_GREEN] (2.35,0) circle (1.9pt);
      \node[UNB_BLUE,font=\tiny,above left=-1pt] at (1.53,0.42) {$b_o$};
      \foreach \xv in {0,1,2,3,4}
        \draw (\xv,0.04)--(\xv,-0.04) node[below,font=\tiny]{\xv};
    \end{tikzpicture}
    \end{center}
    {\footnotesize \textcolor{gray}{Cinza} = uniforme;
    \textcolor{UNB_GREEN}{verde}/\textcolor{UNB_BLUE}{azul} = ótimo. Os
    limiares e níveis são \textbf{realmente diferentes}.}
  \end{columns}
\end{frame}
